import React from 'react';
import { Col } from 'react-bootstrap';

import '../styles/sidebar.css';

const SideBar = () => (
  <Col id="sidebar" xs={3} md={2} lgOffset={500} mdOffset={500}>SideBar</Col>
);

export default SideBar;
